[package]
name = "libforge-core"
version = "0.1.0"
edition = "2021"
publish = false
description = "Core abstractions for LibForge distributions."
license = "Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
